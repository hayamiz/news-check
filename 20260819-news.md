# ニュースまとめ 2026-08-19

対象期間: 過去24時間（2026-08-18〜2026-08-19）

補足: 今回のセッションでも、両サイトへの直接アクセス（WebFetch経由）、および **Jina AI Reader**（`https://r.jina.ai/<URL>`）経由でのアクセスは、いずれもプロキシ側のポリシーによりCONNECT自体が拒否される状態（`EGRESS_BLOCKED`）でした。archive.org（Wayback Machine）経由での代替アクセスも同様に利用できませんでした。そのため、検索エンジン（WebSearch）経由での間接調査のみにより情報収集し、記事タイトル・URL・「〜時間前」等の相対時刻表記を手掛かりに、過去24時間以内の記事かどうかをクロスチェックしています。

---

## Storage Newsletter (https://www.storagenewsletter.com)

### ヘッドライン一覧

今回のセッションでは、検索エンジンの索引上から過去24時間以内（2026-08-18〜08-19付）の新規記事を確認できませんでした。

### 備考

企業名・製品カテゴリ（SSD、HDD、NAND等）・買収/提携キーワードなど複数の切り口で検索しましたが、索引上でヒットする記事は2026-08-12以前のものが中心で、直近の日次記事はまだ索引に反映されていないとみられます（過去の同様のセッションでも同じ傾向を確認済み）。次回セッションで改めて確認します。

---

## Phoronix (https://www.phoronix.com)

### ヘッドライン一覧

- [IOmap Improvement For Linux 7.3 Takes EXT4 & XFS Performance Further](https://www.phoronix.com/news/IOmap-Linux-7.3-Faster)
- [Intel Enables Buffer Compression For Better Gaming Performance With DXVK](https://www.phoronix.com/news/Intel-Buffer-Compression-DXVK) *(検索時点で「10時間前」)*
- [XFS Lands FALLOC_FL_WRITE_ZEROES For Efficiency Improvement In Linux 7.3](https://www.phoronix.com/news/XFS-FALLOC-FL-WRITE-ZEROES) *(検索時点で「10時間前」)*
- [Rust For Linux 7.3 Begins Seeing Fixes To Prepare For Rust's GCC Backend](https://www.phoronix.com/news/Rust-For-Linux-7.3) *(検索時点で「7時間前」)*
- [Linux 7.3 Corrects Faulty Behavior Of FAT File-System Driver For Filenames Too Fat](https://www.phoronix.com/news/Linux-7.3-FAT) *(検索時点で「7時間前」)*
- [Linux 7.3 Adds New "bpf_sock_read_xattr" Feature For systemd, BPF Programs](https://www.phoronix.com/news/Linux-7.3-bpf_sock_read_xattr) *(検索時点で「4時間前」)*

### 重要ニュース要約

#### IOmapの改良でLinux 7.3のEXT4・XFS性能がさらに向上
**URL:** https://www.phoronix.com/news/IOmap-Linux-7.3-Faster

Linux 7.3にマージされたVFS関連プルリクエストの一部として、各種ファイルシステムが利用するIOmapフレームワーク（論理ファイルオフセットと物理ストレージ位置のマッピングを担う）が改良された。

- IOmapがイテレータモデルへの移行を完了し、単一の `iomap_next()` コールバックに統一。間接呼び出しだったものをコンパイラが直接・インライン化可能な呼び出しに変換できるようになった
- PCIe Gen5 NVMe SSDでの小サイズI/Oのボトルネックを解消。EXT4では元のワークロードで1.92M→2.19M IOPSに向上し、libaio queue depth 64以上で約4%、io_uringで約5%、io_uring pollモード（depth 256）では最大10%の性能向上を確認
- Btrfs、EXT2、EROFS、F2FS、GFS2、HPFS、FUSE、exFAT、ZoneFS、NTFS/NTFS3など多数のファイルシステムに対しても同様のIOmap更新が適用されている

#### IntelのDXVKバッファ圧縮対応でゲーム性能が最大5%向上
**URL:** https://www.phoronix.com/news/Intel-Buffer-Compression-DXVK

Mesa 26.3-devel（安定版は2026年11月予定）に、Intel ANVドライバーでのDXVK（Direct3D 8/9/10/11 on Vulkan）向けバッファ圧縮（CCS: Color Control Surface）対応がマージされた。

- Intel Arc Battlemageでは「Shadow of the Tomb Raider」で5.26%、「Total War: Warhammer 3」で4.6%、「Total War Pharaoh」で2%の性能向上
- Intel Xe3「Panther Lake」でも「Shadow of the Tomb Raider」で4.8%向上など、複数タイトルで効果を確認
- 従来はVKD3D（Direct3D 12）やSource2タイトル限定だったバッファ圧縮オプションを、DXVKにも拡大し、最終的には条件分岐（`anv_enable_buffer_comp`オプション）自体を廃止して常時有効化する形に変更

#### systemd向けに新kfunc「bpf_sock_read_xattr」がLinux 7.3にマージ
**URL:** https://www.phoronix.com/news/Linux-7.3-bpf_sock_read_xattr

Linux 7.1でマージされたソケットの拡張属性（xattr）サポートについて、BPFプログラムから効率的に読み取る手段がなかった問題をLinux 7.3で解消。

- Christian Brauner氏が、ソケットのsockfs inodeからロックフリーでユーザー拡張属性を読み取れる `bpf_sock_read_xattr()` kfuncを実装
- systemdはソケットのレート制限や、varlinkレジストリ実装のためのソケットタグ付けにユーザー拡張属性を利用しており、本機能はsystemd/GNOME方面のニーズに応える形

### 備考

上記6件はいずれもLinux 7.3マージウィンドウ関連の記事で、検索時点（相対時刻表記）から過去24時間以内の新規記事と判断しました。なお、以下は同じくLinux 7.3関連やLinuxデスクトップ関連の話題ですが、確認できた時刻情報から今回の対象期間（過去24時間）より前（概ね2026-08-15〜08-17付）と判断し、見出しへの掲載を見送りました。
- Marek Olšák氏によるRADV/ACO最適化パッチ19件（2026-08-15付）
- Con Kolivas氏による "-ck" パッチ・MuQSSスケジューラの復活（2026-08-17付）
- Linux 7.3 SMP改善によるレイテンシ削減（2026-08-17付、Bytedanceエンジニアの提案でSMP関数呼び出しレイテンシを約17msから約1.5msに削減）
- Intel & AMD電源管理ドライバー改善、Linux 7.3向けbinfmt_misc BPF拡張

前回のまとめ（2026-08-16付）以降、8/17・8/18付のまとめファイルは作成されていませんが、今回は依頼どおり直近24時間分のみを対象としています。
